
 //Naming Conventions

    // Camelcase
    let firstName = "Yash";
    // PascalCase
    let LastName = "Bothra";
    //SnakeCase
    let middle_name = "Raj";

    // Using Constant (const keyworld)
    const price = 99;

    // Course Name (String)
    var CourseName = "Java DS/Algo Course";

    // Card Number  (Number)
    var CardNumber = "1234567890";

    // Payment Status (Boolen)
    var isPaid = true;

    //Declarin a Variable
    var modeOfPayment = prompt("mode of Payment?");

    //Output to User
    alert(firstName + " sucessfully bought " + CourseName + "using" + modeOfPayment);